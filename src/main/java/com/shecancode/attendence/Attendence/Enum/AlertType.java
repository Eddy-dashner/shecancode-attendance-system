package com.shecancode.attendence.Attendence.Enum;

public enum AlertType {
    // 3 absences in a row (ABSENT or ABSENT_COMMUNICATED)
    CONSECUTIVE_ABSENCES,
    // 3 absences in total over the program
    TOTAL_ABSENCES
}
